# Flask HTML Table Assignment

This project is a simple Flask web application designed to practice passing data from a route to an HTML template and iterating through a list of dictionaries.

## Prerequisites

Make sure you have Python and Flask installed on your machine.
```bash
pip install Flask